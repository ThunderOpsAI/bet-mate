{
  "name": "bet-mate",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:generate": "pnpm --filter @bet-mate/prisma prisma:generate",
    "db:migrate": "pnpm --filter @bet-mate/prisma prisma:migrate"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
